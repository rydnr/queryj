/*
 * PerSqlTemplatesFeature template metadata.
 */
// The template name (the filename).
name : PerSqlTemplatesFeature.stg;

// The type of template: per-table, per-repository, per-foreign-key, per-custom-result, per-sql.
type : per-sql;

// The output flavor.
output: Cucumber;

// The filename builder.
filename  builder : PerSqlTemplates.feature;

// The package.
package: cucumber.templates;

disabled;