/*
 * PerCustomResultTemplatesTest template metadata.
 */
// The template name (the filename).
name : PerCustomResultTemplatesTest.stg;

// The type of template: per-table, per-CustomResult, per-foreign-key, per-custom-result, per-custom-sql.
type : per-custom-result;

// The output flavor.
output: Java;

// The filename builder.
filename  builder : PerCustomResultTemplatesTest.java;

// The package.
package: cucumber.templates;

disabled;