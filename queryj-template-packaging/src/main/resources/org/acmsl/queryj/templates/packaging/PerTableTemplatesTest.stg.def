/*
 * PerTableTemplatesTest template metadata.
 */
// The template name (the filename).
name : PerTableTemplatesTest.stg;

// The type of template: per-table, per-repository, per-foreign-key, per-custom-result, per-custom-sql.
type : per-repository;

// The output flavor.
output: Java;

// The filename builder.
filename  builder : PerTableTemplatesTest.java;

// The package.
package: cucumber.templates;

disabled;