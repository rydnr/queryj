/*
 * PerSqlTemplatesTest template metadata.
 */
// The template name (the filename).
name : PerSqlTemplatesTest.stg;

// The type of template: per-table, per-repository, per-foreign-key, per-custom-result, per-sql.
type : per-repository;

// The output flavor.
output: Java;

// The filename builder.
filename  builder : PerSqlTemplatesTest.java;

// The package.
package: cucumber.templates;

disabled;