/*
 * PerRepositoryTemplatesTest template metadata.
 */
// The template name (the filename).
name : PerRepositoryTemplatesTest.stg;

// The type of template: per-table, per-repository, per-foreign-key, per-custom-result, per-sql.
type : per-repository;

// The output flavor.
output: Java;

// The filename builder.
filename  builder : PerRepositoryTemplatesTest.java;

// The package.
package: cucumber.templates;

disabled;