- Create a new Java class TemplateTemplateFactory in folder src/main/java/org/acmsl/queryj/templates/packaging/;
- Add comment: Template factory used in the process to build QueryJ's Template sources.;
  - Make it extend TemplatePackagingTemplateFactory<TemplateTemplate<TemplatePackagingContext>, TemplatePackagingContext>;
- Create the overridden createTemplate(TemplatePackagingContext) method:
  - Implement it by returning "new TemplateTemplate(context)";
- Fix import groups;
