/*
 * Example comment.
 */
// The template name (the filename).
name : example.stg;

// The type of template: per-table, per-repository, per-foreign-key, per-custom-result, per-sql.
type : per-table;

// The output flavor.
output: Java;

// The filename builder.
filename  builder : <table.name>Example.java;

// The package.
package: com.foo.bar.dao;