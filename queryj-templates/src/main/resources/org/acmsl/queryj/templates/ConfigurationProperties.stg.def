/*
 * ConfigurationProperties template metadata.
 */
// The template name (the filename).
name : ConfigurationProperties.stg;

// The type of template: per-table, per-repository, per-foreign-key, per-custom-result, per-custom-sql.
type : per-repository;

// The output flavor.
output: properties;

// The filename builder.
filename builder : queryj-<repository.normalized.lowercased>.properties;

// The package.
package: <nothing>;