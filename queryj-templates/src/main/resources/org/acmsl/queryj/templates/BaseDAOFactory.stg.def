/*
 * BaseDAOFactory template metadata.
 */
// The template name (the filename).
name : BaseDAOFactory.stg;

// The type of template: per-table, per-repository, per-foreign-key, per-custom-result, per-custom-sql.
type : per-table;

// The output flavor.
output: Java;

// The filename builder.
filename  builder : Abstract<tableName.capitalized>DAOFactory.java;

// The package.
package: <packageName>.dao.rdb;

// Uncomment to disable.
disabled;