/*
 * BaseResultSetExtractor template metadata.
 */
// The template name (the filename).
name : BaseResultSetExtractor.stg;

// The type of template: per-table, per-repository, per-foreign-key, per-custom-result, per-custom-sql.
type : per-repository;

// The output flavor.
output: Java;

// The filename builder.
filename builder : <repository.capitalized>ResultSetExtractor.java;

// The package.
package: <packageName>.dao.<repository.normalized.lowercased>;

disabled;