/*
 * ValueObjectFactory template metadata.
 */
// The template name (the filename).
name : ValueObjectFactory.stg;

// The type of template: per-table, per-repository, per-foreign-key, per-custom-result, per-sql.
type : per-table;

// The output flavor.
output: Java;

// The filename builder.
filename  builder : <tableName.voName>ValueObjectFactory.java;

// The package.
package: <packageName>.vo;