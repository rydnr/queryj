/*
 * DAOFactoryHelper template metadata.
 */
// The template name (the filename).
name : DAOFactoryHelper.stg;

// The type of template: per-table, per-repository, per-foreign-key, per-custom-result, per-custom-sql.
type : per-table;

// The output flavor.
output: Java;

// The filename builder.
filename  builder : <tableName.capitalized>DAOFactoryHelper.java;

// The package.
package: <packageName>.dao.core;

disabled;