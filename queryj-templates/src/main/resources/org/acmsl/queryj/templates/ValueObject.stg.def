/*
 * ValueObject template metadata.
 */
// The template name (the filename).
name : ValueObject.stg;

// The type of template: per-table, per-repository, per-foreign-key, per-custom-result, per-custom-sql.
type : per-table;

// The output flavor.
output: Java;

// The filename builder.
filename  builder : <tableName.voName>ValueObject.java;

// The package.
package: <packageName>.vo;